#pragma once

// Game
#include "Common.h"

// Our game data is very simple, so what would usually be represented by hashes
// of unique names is for now just a list of hand-written numbers
namespace ItemDataID
{
    static constexpr DataID Flag = 0;
    static constexpr DataID Box = 1;
}
